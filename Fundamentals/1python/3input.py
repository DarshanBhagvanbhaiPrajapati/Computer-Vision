"""input takes always captured as string example if we take a integer in 
input say roll number then also it take in form of string for that we use 
typecasting==converting one data type into another"""

name = input("ente your name\n")
print(name)
age = int(input("entre your age"))
print(type(age))