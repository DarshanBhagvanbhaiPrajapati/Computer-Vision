# we have define float than input because it is TYPECASTING

celcius=float(input("entre the temperature in c: "))

fehrenite = (celcius * ( 9/5 )) + 32

print("fehrenite from celcius is: ", fehrenite)